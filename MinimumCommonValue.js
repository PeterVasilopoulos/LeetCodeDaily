var getCommon = function(nums1, nums2) {
    // object to hold occurences of values
    let values = {}

    // loop through both arrays
    for(let i = 0; i < nums1.length || nums2.length; i++) {
        
    }
}